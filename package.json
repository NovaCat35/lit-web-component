{
  "name": "@novacat35/my-monorepo",
  "version": "0.0.2",
  "private": false,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "^5.0.0",           
    "vite": "^latest",                
    "eslint": "^latest",              
    "prettier": "^latest"          
  }
}
